version: '2'

services:
  postgres:
    image: postgres:latest
    container_name: korepetytio
    environment:
      POSTGRES_DB: korepetytio
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
